configure gnome extensions:
  - set status area horizontal spacing
  - dash to dock:
    - set dock icons
    - icon size limit 32
    - shrink the dash
    - dynamic opacity
  - tray icons: reloaded
    - icon margin 4

disable shortcuts conflicting with pop shell:
  - windows:
    - set close window to Super+Q
    - disable hide window
    - disable maximize window
    - disable restore window
    - disable view split on left/right

settings:
  - set profile picture
  - set default applications
  - add pinyin input
  - set time to am/pm
  - set device name

firefox:
  - install extensions
  - set default search engine
  - set catppuccin theme

obsidian:
  - sync vault
  - set catppuccin theme

